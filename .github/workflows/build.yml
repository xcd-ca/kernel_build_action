name: Build Xiaomi 11 Star Kernel (Fix Linker & Vibration)
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 1. 安装基础依赖
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential bc bison flex libssl-dev libelf-dev clang lld llvm tar cpio perl python3 git zip wget

      - name: 2. 拉取源码 (Star 5.4)
        run: |
          git clone --depth=1 -b miui-t https://github.com/tanz3/kernel_xiaomi_sm8350-miui kernel
          git clone --depth=1 https://github.com/kdrag0n/proton-clang clang
          git clone --depth=1 https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9 -b android12-release gcc64
          git clone --depth=1 https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.9 -b android12-release gcc32

      - name: 3. 集成 SukiSU (修复分支参数)
        run: |
          cd kernel
          # 不再传递 nongki，直接运行脚本
          curl -LSs -f "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash
          echo "✅ SukiSU 代码集成成功！"

      - name: 4. 核心修复：强制注入补丁 & 锁定震动身份证
        run: |
          cd kernel
          # 1. 【核心】修复 KernelSU 在 5.4 内核上的代码错误
          # 必须直接指向 KernelSU 目录，或者使用 -L 参数跟随软链接
          echo "[+] 正在修复 TWA_RESUME 兼容性..."
          if [ -d "KernelSU" ]; then
            find KernelSU -type f -name "*.c" -exec sed -i 's/TWA_RESUME/TWA_SIGNAL/g' {} +
            find KernelSU -type f -name "*.h" -exec sed -i 's/TWA_RESUME/TWA_SIGNAL/g' {} +
          fi
          # 兜底：对 drivers/kernelsu 路径再做一次（跟随软链接）
          find -L drivers/kernelsu -type f -name "*.c" -exec sed -i 's/TWA_RESUME/TWA_SIGNAL/g' {} + || true

          # 2. 强制修正版本号（确保没有 + 号，找回草莓味震动）
          sed -i 's/^SUBLEVEL =.*/SUBLEVEL = 233/' Makefile
          echo "echo \"\"" > scripts/setlocalversion
          chmod +x scripts/setlocalversion

          # 3. 彻底清理并重新生成 star_merged_defconfig
          # 先删除旧的 LOCALVERSION 防止重复冲突
          sed -i '/CONFIG_LOCALVERSION/d' arch/arm64/configs/vendor/xiaomi_QGKI.config
          
          cat arch/arm64/configs/vendor/lahaina-qgki_defconfig \
              arch/arm64/configs/vendor/xiaomi_QGKI.config \
              arch/arm64/configs/vendor/star_QGKI.config > arch/arm64/configs/star_merged_defconfig
          
          {
            echo "CONFIG_SUKI=y"
            echo "CONFIG_LOCALVERSION=\"-qgki-gbb70cde46897\""
            echo "CONFIG_LOCALVERSION_AUTO=n"
            echo "CONFIG_STACKPROTECTOR=n"
            echo "CONFIG_STACKPROTECTOR_STRONG=n"
          } >> arch/arm64/configs/star_merged_defconfig

      - name: 5. 纯净编译 (全力冲刺)
        run: |
          cd kernel
          # 再次清理环境隔离
          rm -f $GITHUB_WORKSPACE/clang/bin/ld*
          export PATH=$GITHUB_WORKSPACE/clang/bin:$GITHUB_WORKSPACE/gcc64/bin:$GITHUB_WORKSPACE/gcc32/bin:$PATH
          
          # 刷新配置
          make O=out ARCH=arm64 star_merged_defconfig
          yes "" | make O=out ARCH=arm64 olddefconfig
          
          # 正式编译 Image 和 dtbs
          make -j$(nproc --all) O=out ARCH=arm64 \
               CC=clang HOSTCC=gcc HOSTCXX=g++ \
               LD=ld.lld AR=llvm-ar NM=llvm-nm \
               OBJCOPY=llvm-objcopy OBJDUMP=llvm-objdump STRIP=llvm-strip \
               KCFLAGS="-Wno-implicit-function-declaration -Wno-strict-prototypes -fno-stack-protector" \
               CLANG_TRIPLE=aarch64-linux-gnu- \
               CROSS_COMPILE=aarch64-linux-android- \
               CROSS_COMPILE_ARM32=arm-linux-androideabi- \
               KBUILD_BUILD_USER="Xiaomi" \
               KBUILD_BUILD_HOST="Official" \
               LOCALVERSION="-qgki-gbb70cde46897" \
               Image dtbs

      - name: 6. 提取镜像 (全盘暴力搜索)
        run: |
          mkdir -p output
          # 1. 提取内核本体 (已经确定编译成功了)
          if [ -f kernel/out/arch/arm64/boot/Image ]; then
            cp kernel/out/arch/arm64/boot/Image output/
            echo "✅ 成功找到内核 Image"
          fi
          
          # 2. 暴力搜索 dtbo 文件
          echo "🔍 正在全盘搜寻 dtbo 文件..."
          
          # 直接从 out 根目录开始搜，不管它在哪个文件夹
          # 我们找名字包含 star 且以 .dtbo 结尾的文件
          DTBO_PATH=$(find kernel/out -name "*star*.dtbo" | head -n 1)
          
          # 如果没找到带 star 的，就搜任何包含 lahaina 的 (小米11是 lahaina 平台)
          if [ -z "$DTBO_PATH" ]; then
            DTBO_PATH=$(find kernel/out -name "*lahaina*.dtbo" | head -n 1)
          fi

          if [ ! -z "$DTBO_PATH" ]; then
            echo "🎯 成功定位 DTBO 文件: $DTBO_PATH"
            cp "$DTBO_PATH" output/dtbo.img
          else
            echo "❌ 警告：仍未找到 .dtbo 文件！"
            echo "目前已生成的文件列表如下(调试用):"
            find kernel/out -name "*.dtbo" || echo "确实没有任何 .dtbo 生成"
          fi

          # 3. 检查并展示最终产物
          echo "📦 最终产物列表:"
          ls -lh output/

      - name: 7. 上传
        uses: actions/upload-artifact@v4
        with:
          name: Mi11-Star-VibeFix-Images
          path: output/*
